import ApplicantModal from "./ApplicantModal";

// Thin wrapper to satisfy naming; reuses the existing ApplicantModal UI.
const AddApplicantModal = (props) => <ApplicantModal {...props} />;

export default AddApplicantModal;
